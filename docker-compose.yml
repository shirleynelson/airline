version: '3'

services:
    db:
        image: postgres
    migration:
        build: .
        command: python3 manage.py migrate
        volumes:
            - .:/usr/src/app
        depends_on:
            - db
    web:
        build: .
        command: python3 manage.py runserver 127.0.0.1:8000
        volumes:
            - .:/usr/src/app
        ports:
            - "8000:5000"
        depends_on:
            - db
            - migration
